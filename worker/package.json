{
  "name": "splatoon3-m5paper-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test/",
    "fixtures": "node ../tools/fetch_fixtures.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
