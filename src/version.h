// Firmware version, injected by scripts/version.py from
// platformio.ini custom_fw_version (single source of truth).

#pragma once

#ifndef FW_VERSION
#define FW_VERSION "0.0.0-dev"  // built without the PIO pre-script
#endif
